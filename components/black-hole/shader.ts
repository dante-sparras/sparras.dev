/**
 * Stable import path for the black-hole TSL shader.
 * Implementation lives in `./shader/*`.
 */
export {
  CONFIG_SCALAR_KEYS,
  createBlackHoleShader,
  type BlackHoleUniforms,
  type UniformNode,
  MARCH,
  DISK,
  GRADE,
} from "./shader/index";
